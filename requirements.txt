# Core scraping + data handling (main.py and find_fcc.py)
requests>=2.31.0
pandas>=2.2.0
tabulate>=0.9.0
fake-useragent>=1.5.0
rich>=12.6.0

# TLS/JA3 impersonation to get past Akamai Bot Manager on apps.fcc.gov.
# find_fcc.py prefers this (Chrome impersonation); it falls back to plain
# requests if curl_cffi is unavailable, but the fallback gets tarpitted.
curl_cffi>=0.10.0
